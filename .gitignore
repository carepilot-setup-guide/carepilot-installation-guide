# Full-quality source backup — too large for GitHub (>100MB), not used by the site
assets/use-video-original.mp4

# Original raw video uploads — not referenced by the site, kept as a local backup only
Videos/

# Original screenshot uploads — duplicated (with better filenames) in assets/
extension/

# Build intermediates from compressing video for the size-capped Claude Artifact link
assets/compressed-*.mp4
assets/ffmpeg2pass-0.log*
